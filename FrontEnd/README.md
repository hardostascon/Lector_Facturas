
  # Login app with CRUD

  This is a code bundle for Login app with CRUD. The original project is available at https://www.figma.com/design/jpGOD49yN6AQm9GHWD0RDF/Login-app-with-CRUD.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  